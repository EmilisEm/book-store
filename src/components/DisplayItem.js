import React from "react";
import styles from "../styles/displayItem.module.css";

const DisplayItem = ({ price, img, title, desc }) => {
  return <div className={styles.display}>DisplayItem</div>;
};

export default DisplayItem;
